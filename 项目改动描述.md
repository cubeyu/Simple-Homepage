# 项目改动描述

> 本文件记录"水墨小筑"项目的改动历史，按时间倒序排列。

---

## 2026-07-21 · 修复纵览链接按钮图标大小不统一问题

### 问题现象

纵览区域的链接按钮图标在不同设备上视觉大小不一致：
1. 平板和电脑上图标大小有差异（图标硬编码 18px，按钮高度响应式变化）
2. 不同图标集（mdi、tabler、mingcute 等）本身 viewBox/内边距不同，导致同样尺寸下视觉大小不统一

### 改动文件

| 文件 | 改动内容 |
|------|----------|
| `src/components/LinkBtn.vue` | 新增 `.link-icon-wrapper` 容器统一图标尺寸，图标改为响应式大小 |

### 修复方案

1. **图标容器化**：新增 `.link-icon-wrapper`  span 容器，使用 `inline-flex + justify-content: center` 包裹图标，确保不同 viewBox 的图标在统一尺寸容器内居中显示
2. **响应式图标尺寸**：图标随按钮高度同步变化
   - 移动端（<769px）：18px
   - 平板（≥769px）：20px
   - 桌面（≥1200px）：22px
3. **hover 微缩放**：悬停时图标容器 `scale(1.08)`，增强交互反馈
4. **`flex-shrink: 0`**：防止图标在窄按钮中被挤压变形

---

## 2026-07-21 · README.md 全面更新

### 改动文件

| 文件 | 改动内容 |
|------|----------|
| `README.md` | 全面重写，准确反映项目当前水墨丹青风格设计 |

### 更新要点

1. **项目速览**：核心亮点从 7 项扩充到 11 项，新增五色墨韵、卷轴形制、闲章点缀、代码规范等
2. **技术栈**：补充 ESLint 10.x、Prettier 3.x 等开发工具
3. **项目结构**：更新组件列表（新增 Header/TechStack/TimeProgress/TodoList），补充配置文件说明
4. **新增章节**：「设计语言 · 水墨丹青」章节，详细介绍五色墨韵系统、朱砂印泥、双主题意境
5. **组件特写**：全部重写，准确描述当前水墨风格功能（卷轴主卡片、四角装饰、竖排题字、墨色分割线等）
6. **配置手册**：修正 config.json 示例（移除已废弃的 emjoi 字段），补充图标来源说明
7. **版本路线图**：更新已完成项（12 项），规划中增加字体子集化等

---

## 2026-07-21 · 修复暗色模式白屏问题（Vue scoped CSS :global() 编译 bug）

### 问题现象

点击主题切换按钮切换到暗色模式时，整个页面变成白屏，所有内容不可见。

### 根本原因

Vue scoped CSS 在处理 `:global([theme='dark']) .className` 这种写法时，**后面的 `.className` 部分丢失了**，导致选择器被错误地编译成了 `[theme='dark']`。

由于 `[theme='dark']` 属性是设置在 `<html>` 元素上的，这条错误的 CSS 规则直接作用于 html 元素。其中有一条规则是 `display: none`（原本用于隐藏太阳元素），结果导致整个 `<html>` 元素被隐藏，页面白屏。

### 错误编译示例

```css
/* 源代码（错误写法） */
:global([theme='dark']) .sun {
  display: none;
}

/* 编译后（错误结果） */
[theme='dark'] {
  display: none;
}

/* 正确编译后应该是 */
[theme='dark'] .sun[data-v-xxxxxx] {
  display: none;
}
```

### 改动文件（2 个）

| 文件 | 改动内容 |
|------|----------|
| `src/components/ParticlesBackground.vue` | 所有 `:global([theme='dark']) .xxx` 改为 `:global([theme='dark'] .xxx)`，将整个选择器包裹在 `:global()` 中，共 12 处 |
| `src/components/CustomCursor.vue` | 同样修改 2 处 `:global([theme='dark']) .xxx` 写法 |

### 修复方案说明

将 `:global([theme='dark']) .className` 改为 `:global([theme='dark'] .className)`，把**整个选择器**都放在 `:global()` 中。虽然这样会让选择器变成全局的（失去 scoped 保护），但因为类名是组件特有的，不会与其他组件冲突。

### 验证结果

- 亮色模式 → 暗色模式切换：正常，无白屏
- 暗色模式 → 亮色模式切换：正常
- html 元素 display：始终为 `block`
- body 高度：始终为视口高度
- 太阳元素在暗色模式下正确隐藏
- 所有暗色模式样式（云雾、飞鸟、扁舟、闲章、星月等）正常生效

---

## 2026-07-21 · 修复全部 Bug + 主题切换失灵

### 改动文件（8 个）

| 文件 | 改动内容 |
|------|----------|
| `src/composables/useTheme.js` | localStorage 读写包裹 try-catch（`safeGetTheme`/`safeSetTheme`），隐私模式不再白屏 |
| `src/style.css` | **主题切换元凶**：`[theme='dark'] html`（后代选择器，永不匹配 html 自身）→ `html[theme='dark']`（属性选择器），html 背景色终于能切换 |
| `src/components/CustomCursor.vue` | 删除永不停止的 `animateTrail()`，合并为带 settled+idle 双重停止检测的 `ensureTrailLoop` |
| `src/components/ParticlesBackground.vue` | `v-if`→`v-show`（星月过渡生效）；matchMedia 缓存到模块级；prettier 格式修复 |
| `src/components/Typewriter.vue` | 新增 `startTimeoutId`，watch/onMounted 的启动延迟被保存，onUnmounted 统一清除 |
| `src/App.vue` | `@import url(assets/css/App.css)` → `@import './assets/css/App.css'` |
| `src/components/Header.vue` | 新增 `const infoTags = config.infoTags \|\| {}`，模板引用改为 `infoTags`，字段缺失不显示 undefined |
| `src/components/TimeProgress.vue` | `daysInYearPassed` 的 `Math.ceil` → `Math.floor`，元旦不再显示"1 天" |
| `src/components/TechStack.vue` | `abbr` 截断后加 `…` 省略号 |

### 主题切换失灵根因

`src/style.css` 第 93 行原为 `[theme='dark'] html`，这是**后代选择器**（"有 theme=dark 属性的元素的后代 html"）。但 `<html>` 是根元素，没有父元素，所以这条规则**永远不匹配 html 自身**。导致切换暗色模式时，`<html>` 的 `background-color` 永远卡在亮色 `#f7f3eb`，浏览器画布背景不切换。

修复为 `html[theme='dark']`（属性选择器，直接匹配 html 元素）后，html 和 body 背景都能正确切换。

### 验证结果

- `npm run lint`：0 errors / 3 warnings（可接受 console）
- `npm run build`：通过（36 模块，3.92s，CSS 31.25 kB / JS 109.40 kB）
- dev server `http://127.0.0.1:4191/`：HTTP 200

### 未修（P3 长期优化）

- 字体 2.12MB 子集化
- prefersReducedMotion 实时监听（影响极小）

---

## 2026-07-21 · Bug 勘察（未改动代码）

### 新增文件

- **`BUG报告.md`**：全项目 Bug 勘察报告。

### 勘察范围

ESLint 静态检查 + Vite 生产构建 + 全量源码人工审查（`src/` 21 个文件 + 配置）。

### 发现问题摘要（13 处）

| 级别 | 数量 | 代表问题 |
|------|------|----------|
| 🔴 P1 中等 | 4 | CustomCursor rAF 泄漏、主题切换星月无过渡、localStorage 白屏风险、Typewriter setTimeout 泄漏 |
| 🟡 P2 轻微 | 6 | App.vue @import 路径、matchMedia 重复查询、infoTags 防空、元旦天数、prettier 格式、reduced-motion 缓存 |
| 🔵 P3 优化 | 3 | 字体子集化、abbr 省略号、console 警告 |

### 构建结果

- `npm run lint`：2 errors + 3 warnings（退出码 1）
- `npm run build`：通过（36 模块，3.88s）
- 产物：index.html 2.04 kB / CSS 31.24 kB / JS 108.62 kB / 字体 2,129 kB

### 详细报告

见 [`BUG报告.md`](./BUG报告.md)。
